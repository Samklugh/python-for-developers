#a function is a block of reusable code that performs a specific task. it is reusable, meaning you can call it multiple times without rewriting the code. 

def welcome_message():
    """the function prints a welcome message to the user.""" #this is a docstring that describes what the function does.
    print("Welcome to the Python programming world!")

#no parameter is added because the function does not require any external information to perform its task.
welcome_message()  #calling the function to execute its code and display the welcome message.